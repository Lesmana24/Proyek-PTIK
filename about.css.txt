/* Reset margins, paddings, and box-sizing for all elements */
*::after, *::before {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background-color: #ffeae0; /* Set background color */
}

.back {
    background-color: #ffeae0; /* Background color for the 'back' class */
}

.nav {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 30px 0;
    background-color: #ffeae0; /* Background color for navigation */
}

.nav .logo h1 {
    font-weight: 600;
    font-family: sans-serif;
    color: black; /* Logo text color */
}

/* Hover effect for navigation items */
.li-navbar:hover {
    background-color: orange;
    transition: background-color 0.2s ease-in-out; /* Specific transition for background-color */
    transition-delay: 0.3s;
    border-radius: 10px;
}

.nav .logo h1{
    font-weight: 600;
    font-family: sans-serif;
    color: black;
}

.nav .logo b{
    color: #ff511c;
}

.nav ul{
    display: flex;
    list-style: none;
}

.nav ul li{
    margin-right: 50px;
}

.nav ul li a{
    text-decoration: none;
    color: black;
    font-weight: 500;
    font-family: sans-serif, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    font-size: 25px;
}

.nav .active a ::after{
    content: none;
    width: 50%;
    height: 3px;
    background-color: #ff511c;
    display: flex;
    position: relative;
    margin-left: 10px;
}
